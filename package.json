{
  "devDependencies": {
    "@types/node": "^14.11.2",
    "gts": "^3.1.0",
    "prettier": "2.6.2",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "canonicalize": "^1.0.8",
    "level": "^8.0.0",
    "semver": "^7.3.6"
  }
}
